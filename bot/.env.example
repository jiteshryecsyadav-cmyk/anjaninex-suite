# ===== Namokara WhatsApp Bot config =====
# Copy this file to ".env" and fill values.

# --- PostgreSQL (same DB as the Suite) ---
PGHOST=localhost
PGPORT=5432
PGDATABASE=namokara_dev
PGUSER=postgres
PGPASSWORD=Jitesh@0285

# --- Which firm this bot belongs to (multi-tenant). Get from platform.firms.id ---
FIRM_ID=

# --- Claude (rate/category extraction + Q&A) ---
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-haiku-4-5-20251001

# --- WhatsApp (Baileys) ---
WA_SESSION_DIR=./auth

# --- Behaviour ---
AUTO_FORWARD=false          # true = matched buyers ko auto bhejo; false = sirf store karo
BUYER_SEARCH_LOOKBACK_DAYS=30
PORT=5050
